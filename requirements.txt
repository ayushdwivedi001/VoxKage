# ─────────────────────────────────────────────────────────────────────────────
# VoxKage v1.0.0 — Core Dependencies
# These are installed with `pip install voxkage`
# For optional packs: pip install voxkage[rag,vision,browser,docs_plus,tray,full]
# ─────────────────────────────────────────────────────────────────────────────

# Runtime & wiring
mcp>=1.0
httpx>=0.27
requests>=2.31
python-dotenv>=1.0

# OS integration
psutil>=5.9
pyautogui>=0.9
pygetwindow>=0.0.9

# Windows-only (auto-skipped on macOS)
pywin32>=306; sys_platform=='win32'
winshell>=0.6; sys_platform=='win32'
winotify>=1.1; sys_platform=='win32'
pywinauto>=0.6; sys_platform=='win32'
pycaw>=20240210; sys_platform=='win32'
comtypes>=1.3; sys_platform=='win32'

# Office document read/write (lightweight core)
python-docx>=1.1
openpyxl>=3.1
python-pptx>=0.6
Pillow>=10.0

# Plugin API credentials
spotipy>=2.23
PyGithub>=2.1
google-auth>=2.22
google-auth-oauthlib>=1.1
google-api-python-client>=2.100
python-telegram-bot>=20.0
